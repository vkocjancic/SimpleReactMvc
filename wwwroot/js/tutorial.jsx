class HelloText extends React.Component {
    render() {
        return (
            <p>Hello from react jsx file!</p>
        );
    }
}

//ReactDOM.render(<HelloText />, document.getElementById('reactMessage'));